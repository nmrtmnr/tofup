extend /_layouts/_default

block vars
  -
    id = 'pixi_test'
    page = {
      language: 'ja',
      id: id,
      categories: [],
      path: `/${id}/`,
    }

    moduleName = id

    texts = [
      'T',
      'O',
      'F',
      'U',
      'P',
    ]

block main
  +section(page.id)(data-module=module('visualize'))
    .text
      each text in texts
        span
          span(data-text=text)!=text
    canvas
