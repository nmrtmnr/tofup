extend /_layouts/_default

block vars
  -
    page = {
      language: "ja",
      id: "softbody",
      categories: [],
      path: "/softbody/"
    }

    moduleName = "softbody"

block main

  +section(page.id)(data-module=module(moduleName))
